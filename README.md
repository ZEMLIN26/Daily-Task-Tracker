# Daily Task Tracker

A small web app to track daily job application and LeetCode quotas.

## Run It

Install dependencies:

```sh
npm install
```

Start the Express server:

```sh
npm start
```

Open:

```text
http://localhost:3000
```

The app stores progress in `tracker.db`, which is created automatically from `schema.sql`.
